---
- name: Configuring Cisco switch
  hosts: localhost
  tasks:
    - name: Specify a container name to execute the command on
      redhat.openshift.k8s:
        namespace: aap
        pod: automation-controller-759c89b95c-46tcb
        container: automation-controller-ee
        command: echo "hello"
